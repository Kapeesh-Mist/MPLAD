# Point this at the FastAPI backend (backend/app) once it's running.
VITE_API_BASE_URL=http://localhost:8000/api

# Set to "false" to call the real backend instead of the built-in demo data.
VITE_USE_MOCK=true
